<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Super Admin | ID Card Manager</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Outfit', sans-serif;
        }

        .sidebar-transition {
            transition: width 0.3s ease-in-out;
        }
    </style>
</head>

<body class="bg-slate-50 text-slate-800">
    <!-- Mobile Nav Header -->
    <%- include("../partials/adminHeader.ejs") %>


        <div class="flex h-screen overflow-hidden pt-16 md:pt-0">
            <!-- Sidebar -->

            <%- include("../partials/adminSidebar.ejs") %>

                <!-- Main Content -->
                <main class="flex-1 overflow-y-auto bg-slate-50 p-6 md:p-10">
                    <div class="max-w-7xl mx-auto">
                        <header class="mb-10">
                            <h1 class="text-4xl font-bold text-slate-900 tracking-tight mb-2">Super Admin Dashboard</h1>
                            <p class="text-slate-500 text-lg">Central control for all registered educational
                                institutions.
                            </p>
                        </header>

                        <!-- Stats Overview -->
                        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-10">
                            <div
                                class="bg-white rounded-3xl p-6 shadow-sm border border-rose-50 hover:shadow-md transition-shadow">
                                <div
                                    class="w-12 h-12 bg-rose-100 rounded-2xl flex items-center justify-center text-rose-600 mb-4 font-bold text-xl">
                                    S</div>
                                <p class="text-slate-500 font-medium mb-1">Total Schools</p>
                                <h3 class="text-3xl font-bold text-slate-900" id="schoolCount">
                                    <%= totalSchools %>
                                </h3>
                            </div>
                            <div
                                class="bg-white rounded-3xl p-6 shadow-sm border border-indigo-50 hover:shadow-md transition-shadow">
                                <div
                                    class="w-12 h-12 bg-indigo-100 rounded-2xl flex items-center justify-center text-indigo-600 mb-4 font-bold text-xl">
                                    U</div>
                                <p class="text-slate-500 font-medium mb-1">Total Students</p>
                                <h3 class="text-3xl font-bold text-slate-900" id="allStudentCount">
                                    <%= totalStudents %>
                                </h3>
                            </div>
                            <div
                                class="bg-white rounded-3xl p-6 shadow-sm border border-amber-50 hover:shadow-md transition-shadow">
                                <div
                                    class="w-12 h-12 bg-amber-100 rounded-2xl flex items-center justify-center text-amber-600 mb-4 font-bold text-xl">
                                    P</div>
                                <p class="text-slate-500 font-medium mb-1">Pending Approval</p>
                                <h3 class="text-3xl font-bold text-slate-900" id="pendingApprovalCount">
                                    <%= pendingApprovalCount %>
                                </h3>
                            </div>
                            <div
                                class="bg-white rounded-3xl p-6 shadow-sm border border-emerald-50 hover:shadow-md transition-shadow">
                                <div
                                    class="w-12 h-12 bg-emerald-100 rounded-2xl flex items-center justify-center text-emerald-600 mb-4 font-bold text-xl">
                                    ✓</div>
                                <p class="text-slate-500 font-medium mb-1">Cards Printed</p>
                                <h3 class="text-3xl font-bold text-slate-900" id="printedCount">
                                    <%= printedCount %>
                                </h3>
                            </div>
                        </div>

                        <!-- Recent Schools -->
                        <div class="bg-white rounded-3xl shadow-sm border border-rose-50 overflow-hidden">
                            <div class="p-6 border-b border-rose-50 flex items-center justify-between">
                                <h3 class="text-xl font-bold text-slate-900">Registered Schools</h3>
                                <a href="/superadmin/schools" class="text-rose-600 font-semibold hover:underline">View All</a>
                            </div>
                            <div class="overflow-x-auto">
                                <table class="w-full text-left">
                                    <thead
                                        class="bg-rose-50/50 text-rose-800 text-sm font-bold uppercase tracking-wider">
                                        <tr>
                                            <th class="px-6 py-4">School Name</th>
                                            <th class="px-6 py-4">Students</th>
                                            <th class="px-6 py-4 text-center">Pending id's</th>
                                            <th class="px-6 py-4 text-right">Printed id's</th>
                                        </tr>
                                    </thead>
                                    <tbody class="divide-y divide-rose-50">
                                        <% latestSchools.forEach(school=> { %>
                                            <tr class="hover:bg-rose-50/30 transition-colors">
                                                <td class="px-6 py-4">
                                                    <div class="flex items-center space-x-3">
                                                        <% if (school.logo) { %>
                                                        <img src="<%= school.logo %>" class="w-8 h-8 rounded-lg object-cover">
                                                            <% } else { %>
                                                                <div
                                                                    class="w-8 h-8 rounded-lg bg-indigo-600 flex items-center justify-center text-white font-bold text-[0.65rem]">
                                                                    <%= school.name.slice(0,2).toUpperCase() %>
                                                                </div>
                                                                <% } %>
                                                                <div><p class="font-semibold"><%= school.name %></p></div>
                                                    </div>
                                                </td>
                                                <td class="px-10 py-4 text-slate-600"><%= school.totalStudents %></td>
                                                <td class="px-6 py-4 text-center "><%= school.submittedStudents %></td>
                                                <td class="px-10 py-4 text-right"><%= school.printedtudents %></td>
                                            </tr>
                                            <% }) %>
                                    </tbody>

                                </table>
                            </div>
                        </div>
                    </div>
                </main>
        </div>

        <%- include("../partials/adminHeaderLogic.ejs") %>

</body>

</html>